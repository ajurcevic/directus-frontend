import { QueryBuilder } from 'knex';
import { Query, Filter, SchemaOverview } from '../types';
export default function applyQuery(collection: string, dbQuery: QueryBuilder, query: Query, schema: SchemaOverview): Promise<void>;
export declare function applyFilter(schema: SchemaOverview, rootQuery: QueryBuilder, rootFilter: Filter, collection: string): Promise<void>;
